# AskRepo frontend — copy to `.env.local` and adjust.
# What this means, and why it is not NEXT_PUBLIC_: ../docs/configuration.md

# ── API ───────────────────────────────────────────────────────────────────────
# Read on the server only. Under Docker Compose: http://backend:8000
API_URL=http://localhost:8000
